# ============================================================
# Vaultx environment (template)
# Copy this file to `.env` and fill in your real values.
# `.env` is git-ignored — your keys will NOT be committed.
# ============================================================

# ── Stripe (test mode) ──────────────────────────────────────
# Dashboard: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxx
# From `stripe listen --forward-to localhost:8080/api/payments/webhook`
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxx
STRIPE_CURRENCY=usd
CHECKOUT_SUCCESS_URL=http://localhost:5173/wallet
CHECKOUT_CANCEL_URL=http://localhost:5173/wallet
